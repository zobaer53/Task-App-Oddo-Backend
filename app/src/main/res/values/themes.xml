<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base launch theme. The real UI styling is applied in Compose via AppTheme. -->
    <style name="Theme.OdooTasks" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
